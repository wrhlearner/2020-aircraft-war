#pragma once

#include "etlbase.h"
class planeviewmodel;
class bulletviewmodel;

class modelsinks:public IPropertyNotification{
public:
	planemodelSink(planeviewmodel* pm) throw();
	bulletmodelSink(bulletviewmodel* bm) throw();

	virtual void OnPropertyChanged(const std::string& str);

private:
	planeview* m_pm;
	bulletview* m_bm;
};
